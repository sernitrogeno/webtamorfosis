/**
 * Fotografías reales para las maquetas "después".
 * -----------------------------------------------------------------------
 * Deja la cadena vacía para usar el fondo de degradado (CSS).
 * Para usar una foto real, coloca el archivo en `public/mockups/` y pon aquí
 * su ruta pública, por ejemplo: "/mockups/restaurant.jpg".
 *
 * Recomendado: imágenes horizontales (~1200×675) optimizadas.
 */
export const mockupPhotos: Record<"restaurant" | "clinic" | "reform", string> = {
  restaurant: "", // p. ej. "/mockups/restaurant.jpg"
  clinic: "", // p. ej. "/mockups/clinic.jpg"
  reform: "", // p. ej. "/mockups/reform.jpg"
};
